'use server'

async function signIn() {
    try {
        
    } catch (error) {
        console.error('Error', error);
    }
}

async function signUp(userData: SignUpParams) {
    try {
        
    } catch (error) {
        console.error('Error', error);
    }
}

export {
    signIn,
    signUp,
}